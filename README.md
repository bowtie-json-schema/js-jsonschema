# js-jsonschema

A [Bowtie](https://github.com/bowtie-json-schema/bowtie) test harness.

Its image is published to `ghcr.io/bowtie-json-schema/js-jsonschema` and run via `bowtie run -i js-jsonschema`.
